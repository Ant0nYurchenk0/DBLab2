<div class="container">

    <h1>
        Restaurant Database
    </h1>
    <a class="btn btn-block btn-primary " href="@Url.Action("Index", "Menus")">
        Menus
    </a>
    <a class="btn btn-block btn-primary" href="@Url.Action("Index", "Dishes")">
        Dishes
    </a>
    <a class="btn btn-block btn-primary" href="@Url.Action("Index", "Ingredients")">
        Ingredients
    </a>
    <a class="btn btn-block btn-primary" href="@Url.Action("Index", "Orders")">
        Orders
    </a>
    <a class="btn btn-block btn-primary" href="@Url.Action("Index", "Seasons")">
        Seasons
    </a>
    <a class="btn btn-block btn-primary" href="@Url.Action("Index", "Query")">
        Queries
    </a>
</div>

<style>
    .container{
        text-align: center;
        align-content: center;
        
    }
    .btn-block{
        text-align: center;
        max-width: 250px;
        margin: auto;
        font-size: 2.5rem;
        height: 50px;
    }
</style>