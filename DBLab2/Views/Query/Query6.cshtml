@model DBLab2.Queries.ViewModels.Query6ViewModel
@{
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Query</h2>
@using (Html.BeginForm("Result6", "Query"))
{
    @Html.LabelFor(q => q.MenuId)
    @Html.DropDownListFor(q => q.MenuId,
         new MultiSelectList(Model.Menus,
         "Id",
         "Name"),
         "Select Menu",
         new { @class = "form-control " })
    <p></p>
    @Html.ValidationMessageFor(q => q.MenuId)
    @Html.AntiForgeryToken();
    <button type="submit" class="btn btn-success">Submit</button>
    <a class="btn btn-primary" href="@Url.Action("Index", "Query")">
        Cancel
    </a>
}
