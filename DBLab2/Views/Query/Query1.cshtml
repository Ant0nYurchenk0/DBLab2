@model DBLab2.Queries.ViewModels.Query1ViewModel
@{
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Query</h2>
@using (Html.BeginForm("Result1", "Query"))
{
    @Html.LabelFor(q => q.MenuName)
    @Html.DropDownListFor(q => q.MenuName,
         new MultiSelectList(Model.Menus,
         "Name",
         "Name"),
         "Select Menu",
         new { @class = "form-control " })
    <p></p>
    @Html.ValidationMessageFor(q => q.MenuName)
    @Html.AntiForgeryToken();
    <button type="submit" class="btn btn-success">Submit</button>
    <a class="btn btn-primary" href="@Url.Action("Index", "Query")">
        Cancel
    </a>
}
