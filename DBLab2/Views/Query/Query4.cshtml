@model DBLab2.Queries.ViewModels.Query4ViewModel
@{
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Query</h2>
@using (Html.BeginForm("Result4", "Query"))
{
    @Html.LabelFor(q => q.SeasonName)
    @Html.DropDownListFor(q => q.SeasonName,
         new MultiSelectList(Model.Seasons,
         "Name",
         "Name"),
         "Select Season",
         new { @class = "form-control " })
    <p></p>
    @Html.ValidationMessageFor(q => q.SeasonName)
    @Html.AntiForgeryToken();
    <button type="submit" class="btn btn-success">Submit</button>
    <a class="btn btn-primary" href="@Url.Action("Index", "Query")">
        Cancel
    </a>
}
