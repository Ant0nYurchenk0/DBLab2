@model DBLab2.Queries.ViewModels.Query5ViewModel
@{
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Query</h2>
@using (Html.BeginForm("Result5", "Query"))
{
    @Html.LabelFor(q => q.IngredientName)
    @Html.DropDownListFor(q => q.IngredientName,
         new MultiSelectList(Model.Ingredients,
         "Name",
         "Name"),
         "Select Ingredient",
         new { @class = "form-control " })
    <p></p>
    @Html.ValidationMessageFor(q => q.IngredientName)
    @Html.AntiForgeryToken();
    <button type="submit" class="btn btn-success">Submit</button>
    <a class="btn btn-primary" href="@Url.Action("Index", "Query")">
        Cancel
    </a>
}
